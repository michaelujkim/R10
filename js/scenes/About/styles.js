import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  container: {
    padding: 20

  },
  heading: {
    paddingTop: 20,
    paddingBottom: 20,
    fontWeight: "bold",
    fontSize: 25
  }
});
